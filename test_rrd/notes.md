TDD 
 red: 
  input: spec.md, cd_structure.toon
  validation: All files are created compared with cd_structure.toon 
 green:
  
 blue

In every process, it should guanrantee the following
 No bug (by using tools: cdscan, cdqa, dbgctxt)
 TOON format (by using tool: pydantic_toon)
 agentic LLM calling (using ocwrapper)

